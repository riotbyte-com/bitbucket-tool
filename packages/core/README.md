# @bitbucket-tool/core

Internal shared package for [bitbucket-tool](https://github.com/riotbyte-com/bitbucket-tool). Provides the Bitbucket Cloud API client, service layer, authentication, and types used by the CLI and MCP server.

Not published to npm — bundled into the consumer packages at build time.
